{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Debug CMake Target",
            "type": "lldb",
            "request": "launch",
            // Resolved by CMake Tools:
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}